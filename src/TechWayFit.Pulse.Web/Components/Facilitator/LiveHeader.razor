@using TechWayFit.Pulse.Contracts.Enums
@using TechWayFit.Pulse.Contracts.Responses
@inject NavigationManager Navigation

<!-- Slim top bar -->
<div class="live-top-bar">

    <!-- Sidebar toggle -->
    <button class="live-sidebar-toggle" @onclick="OnToggleSidebar" title="@(IsSidebarCollapsed ? "Show activities" : "Hide activities")">
        <i class="fas fa-bars"></i>
    </button>

    <div class="live-top-bar-divider d-none d-md-block"></div>

    <!-- Breadcrumb + title -->
    <div class="d-flex flex-column justify-content-center" style="min-width:0;">
        <a href="/facilitator/dashboard" class="text-muted small text-decoration-none d-none d-md-block"
           style="font-size: 0.68rem; line-height: 1;">
            <i class="fas fa-th-large me-1"></i>Dashboard
        </a>
        <span class="live-top-bar-title">@Session.Title</span>
    </div>

    <!-- Join code pill -->
    <span class="badge d-none d-md-inline-flex align-items-center gap-1"
          style="background: var(--pulse-gradient); color: white; font-size: 0.75rem; font-family: var(--pulse-font-mono); letter-spacing: 0.05em; padding: 0.3rem 0.7rem; border-radius: 999px; flex-shrink: 0;">
        <i class="fas fa-link" style="font-size: 0.65rem;"></i>
        @SessionCode
    </span>

    <!-- Stat pills -->
    <div class="live-top-bar-stats d-none d-lg-flex align-items-center gap-2">
        <span class="live-stat-pill live-stat-pill--users">
            <i class="fas fa-users"></i> @ParticipantCount
        </span>
        <span class="live-stat-pill live-stat-pill--total">
            <i class="fas fa-tasks"></i> @Activities.Count
        </span>
        <span class="live-stat-pill live-stat-pill--active">
            <i class="fas fa-play-circle"></i> @Activities.Count(a => a.Status == TechWayFit.Pulse.Contracts.Enums.ActivityStatus.Open)
        </span>
        <span class="live-stat-pill live-stat-pill--done">
            <i class="fas fa-check-circle"></i> @Activities.Count(a => a.Status == TechWayFit.Pulse.Contracts.Enums.ActivityStatus.Closed)
        </span>
    </div>

    <!-- Live badge -->
    @if (Session.Status == SessionStatus.Live)
    {
        <span class="badge bg-success d-none d-sm-inline px-2 py-1" style="flex-shrink:0; font-size: 0.7rem;">
            <span class="me-1" style="display:inline-block; width:6px; height:6px; border-radius:50%; background:white; animation: live-dot-pulse 1.5s ease-in-out infinite; vertical-align: middle;"></span>
            LIVE
        </span>
    }

    <!-- Action buttons -->
    <div class="live-top-bar-actions">
        <button class="btn btn-sm btn-outline-secondary d-none d-md-inline-flex align-items-center gap-1" @onclick="OnShowEditSession">
            <i class="fas fa-edit"></i>
            <span class="d-none d-lg-inline">Edit</span>
        </button>
        <button class="btn btn-sm btn-outline-info d-none d-md-inline-flex align-items-center gap-1" @onclick="NavigateToReport">
            <i class="fas fa-file-lines"></i>
            <span class="d-none d-lg-inline">Report</span>
        </button>
        <button class="btn btn-sm btn-outline-primary d-flex align-items-center gap-1" @onclick="OnShowQRModal">
            <i class="fas fa-qrcode"></i>
            <span class="d-none d-sm-inline">Join</span>
        </button>
        @if (Session.Status == SessionStatus.Live)
        {
            <button class="btn btn-sm btn-danger" @onclick="OnEndSession" disabled="@IsPerformingAction">
                <i class="fas fa-stop me-1 d-none d-sm-inline"></i>
                @(IsPerformingAction ? "Ending..." : "End")
            </button>
        }
        else if (Session.Status == SessionStatus.Draft)
        {
            <button class="btn btn-sm btn-success" @onclick="OnStartSession" disabled="@IsPerformingAction">
                <i class="fas fa-play me-1 d-none d-sm-inline"></i>
                @(IsPerformingAction ? "Starting..." : "Start")
            </button>
        }
    </div>
</div>

@code {
    [Parameter, EditorRequired]
    public SessionSummaryResponse Session { get; set; } = default!;

    [Parameter, EditorRequired]
    public string SessionCode { get; set; } = default!;

    [Parameter]
    public int ParticipantCount { get; set; }

    [Parameter, EditorRequired]
    public List<AgendaActivityResponse> Activities { get; set; } = new();

    [Parameter]
    public bool IsSidebarCollapsed { get; set; }

    [Parameter]
    public EventCallback OnToggleSidebar { get; set; }

    [Parameter]
    public bool IsPerformingAction { get; set; }

    [Parameter]
    public EventCallback OnShowQRModal { get; set; }

    [Parameter]
    public EventCallback OnShowEditSession { get; set; }

    [Parameter]
    public EventCallback OnStartSession { get; set; }

    [Parameter]
    public EventCallback OnEndSession { get; set; }

    private void NavigateToReport()
    {
        Navigation.NavigateTo($"/facilitator/report/{SessionCode}", forceLoad: true);
    }
}