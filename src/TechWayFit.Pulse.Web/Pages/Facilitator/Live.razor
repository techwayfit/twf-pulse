@page "/facilitator/live"
@using TechWayFit.Pulse.Web.Services
@using TechWayFit.Pulse.Contracts.Responses
@using TechWayFit.Pulse.Contracts.Enums
@using Microsoft.Extensions.Logging
@inject NavigationManager Navigation
@inject IJSRuntime JS
@inject IPulseApiService ApiService
@inject ILogger<Live> Logger
@rendermode InteractiveServer

<main class="py-5">
    <!-- Constrained Container -->
    <div class="container" style="max-width: 1200px;">
@if (isLoading)
{
            <div class="card border-0 shadow-sm">
                <div class="card-body text-center py-5">
                    <div class="spinner-border text-primary" role="status">
                        <span class="visually-hidden">Loading...</span>
                    </div>
                    <p class="mt-3 text-muted mb-0">Loading session...</p>
                </div>
            </div>
}
else if (!string.IsNullOrEmpty(errorMessage))
{
            <div class="card border-0 shadow-sm">
                <div class="card-body p-4">
                    <div class="alert alert-danger mb-4" role="alert">
                        @errorMessage
                    </div>
                    <a href="/" class="btn btn-primary pulse-btn-primary">Back to Home</a>
                </div>
            </div>
}
else if (session != null)
{
            <div class="d-flex justify-content-between align-items-center mb-4 flex-wrap gap-3">
                <div>
                    <h2 class="mb-2">@session.Title</h2>
                    <p class="text-muted mb-0">
                        <span class="me-3">Code: <strong>@sessionCode</strong></span>
                        <span class="me-3">Participants: <strong>@participantCount</strong></span>
                        <span>TTL: <strong>6h</strong></span>
                    </p>
                </div>
                <div class="d-flex gap-2 align-items-center">
                    <span class="badge bg-success bg-opacity-10 text-success border border-success border-opacity-25 px-3 py-2">
                        <span class="pulse-dot me-2"></span>
                        Live
                    </span>
                    <button class="btn btn-outline-danger" @onclick="EndSession" disabled="@isPerformingAction">
                        @(isPerformingAction ? "Ending..." : "End Session")
                    </button>
                </div>
            </div>

            <div class="row g-4">
                <!-- Left Sidebar: QR Code Only -->
                <div class="col-lg-4">
                    <!-- QR Code Section -->
                    <div class="card border-0 shadow-sm mb-4">
                        <div class="card-body p-4">
                            <h5 class="card-title fw-semibold mb-4">Participant Join</h5>

                            <!-- QR Code Display -->
                            <div class="text-center mb-4">
                                <div class="bg-white border rounded-3 p-3 d-inline-block">
                                    <!-- Auto-generates QR code via data-qr-url attribute -->
                                    <canvas id="qr-@(sessionCode.Replace("-", ""))"
                                            data-qr-url="@joinUrl"
                                            width="200"
                                            height="200"></canvas>
                                </div>

                                <!-- Manual Generate Button -->
                                <div class="mt-3">
                                    <button class="btn btn-sm btn-outline-primary"
                                            @onclick="ManualGenerateQR"
                                            disabled="@isGeneratingQR">
                                        @if (isGeneratingQR)
                                        {
                                            <span class="spinner-border spinner-border-sm me-1" role="status"></span>
                                            <span>Generating...</span>
                                        }
                                        else
                                        {
                                            <span>🔄 Generate QR Code</span>
                                        }
                                    </button>
                                </div>

                                @if (!string.IsNullOrEmpty(errorMessage) && errorMessage.Contains("QR"))
                                {
                                    <div class="alert alert-warning mt-2 small" role="alert">
                                        @errorMessage
                                    </div>
                                }
                            </div>

                            <!-- Join URL -->
                            <div class="mb-3">
                                <label class="form-label fw-medium small text-muted">Join URL</label>
                                <div class="input-group">
                                    <input type="text" class="form-control form-control-sm" readonly value="@joinUrl" id="joinUrlInput" />
                                    <button class="btn btn-sm btn-outline-secondary" @onclick="CopyJoinUrl" title="Copy to clipboard">
                                        📋
                                    </button>
                                </div>
                            </div>

                            <!-- Session Code -->
                            <div class="text-center">
                                <div class="bg-primary bg-opacity-10 rounded-3 px-4 py-3 mb-2">
                                    <div class="h3 mb-0 text-primary fw-bold">@sessionCode</div>
                                </div>
                                <p class="small text-muted mb-0">Session Code</p>
                            </div>
                        </div>
                    </div>

                    <!-- Session Info Card -->
                    <div class="card border-0 shadow-sm">
                        <div class="card-body p-4">
                            <h5 class="card-title fw-semibold mb-3">Session Info</h5>
                            <div class="d-flex flex-column gap-2">
                                <div class="d-flex justify-content-between">
                                    <span class="text-muted">Participants:</span>
                                    <strong>@participantCount</strong>
                                </div>
                                <div class="d-flex justify-content-between">
                                    <span class="text-muted">Activities:</span>
                                    <strong>@activities.Count</strong>
                                </div>
                                <div class="d-flex justify-content-between">
                                    <span class="text-muted">Active:</span>
                                    <strong>@activities.Count(a => a.Status == Contracts.Enums.ActivityStatus.Open)</strong>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Main Content: Current Activity + All Activities -->
                <div class="col-lg-8">
                    <!-- Current Activity (if open) -->
                    @if (currentActivity != null)
                    {
                        <div class="card border-0 shadow-sm mb-4 border-success">
                            <div class="card-body p-4">
                                <div class="d-flex justify-content-between align-items-start mb-3">
                                    <div>
                                        <h5 class="card-title fw-semibold mb-2">
                                            <span class="badge bg-success me-2">
                                                <span class="pulse-dot me-1"></span>LIVE
                                            </span>
                                            @currentActivity.Title
                                        </h5>
                                        <p class="text-muted small mb-0">@(currentActivity.Prompt ?? "No prompt")</p>
                                    </div>
                                    <div class="d-flex align-items-center gap-2">
                                        <span class="small text-muted">Order: #@currentActivity.Order</span>
                                    </div>
                                </div>

                                <!-- Placeholder for live preview -->
                                <div class="bg-light rounded-3 p-4 mb-3 text-center">
                                    <p class="text-muted mb-0">
                                        <i class="fas fa-chart-bar me-2"></i>
                                        Live responses will appear here (dashboard coming soon)
                                    </p>
                                </div>

                                <!-- Activity Controls -->
                                <div class="d-flex flex-wrap gap-2 align-items-center">
                                    <!-- Go Back Button -->
                                    @if (HasPreviousActivity())
                                    {
                                        <button class="btn btn-outline-secondary"
                                                @onclick="GoBack"
                                                disabled="@isPerformingAction">
                                            <i class="fas fa-arrow-left me-1"></i>
                                            @(isPerformingAction ? "Moving..." : "Go Back")
                                        </button>
                                    }

                                    <!-- Close Button -->
                                    <button class="btn btn-warning"
                                            @onclick="CloseCurrentActivity"
                                            disabled="@isPerformingAction">
                                        <i class="fas fa-stop me-1"></i>
                                        @(isPerformingAction ? "Closing..." : "Close Activity")
                                    </button>

                                    <!-- Move Next Button -->
                                    @if (HasNextActivity())
                                    {
                                        <button class="btn btn-primary pulse-btn-primary"
                                                @onclick="MoveNext"
                                                disabled="@isPerformingAction">
                                            @(isPerformingAction ? "Moving..." : "Next Activity")
                                            <i class="fas fa-arrow-right ms-1"></i>
                                        </button>
                                    }
                                    else
                                    {
                                        <span class="badge bg-info ms-auto">Last Activity</span>
                                    }

                                    <a class="btn btn-outline-primary ms-auto" href="/facilitator/dashboards">
                                        <i class="fas fa-chart-bar me-1"></i>View Dashboard
                                    </a>
                                </div>
                            </div>
                        </div>
                    }

                    <!-- All Activities -->
                    <div class="card border-0 shadow-sm mb-4">
                        <div class="card-body p-4">
                            <div class="d-flex justify-content-between align-items-center mb-4">
                                <h5 class="card-title fw-semibold mb-0">Workshop Activities</h5>
                                <span class="badge bg-primary bg-opacity-10 text-primary">@activities.Count total</span>
                            </div>

                            @if (activities != null && activities.Any())
                            {
                                <div class="d-flex flex-column gap-3">
                                    @foreach (var (activity, index) in activities.OrderBy(a => a.Order).Select((a, i) => (a, i)))
                                    {
                                        <div class="card @(activity.Status == Contracts.Enums.ActivityStatus.Open ? "border-success" : "") shadow-sm">
                                            <div class="card-body">
                                                <div class="d-flex justify-content-between align-items-start mb-2">
                                                    <div class="d-flex align-items-center gap-2">
                                                        <span class="badge bg-light text-dark">#@activity.Order</span>
                                                        <strong class="fw-semibold">@activity.Type</strong>
                                                    </div>
                                                    @if (activity.Status == Contracts.Enums.ActivityStatus.Open)
                                                    {
                                                        <span class="badge bg-success">
                                                            <span class="pulse-dot me-1"></span>Live
                                                        </span>
                                                    }
                                                    else if (activity.Status == Contracts.Enums.ActivityStatus.Closed)
                                                    {
                                                        <span class="badge bg-secondary">Completed</span>
                                                    }
                                                    else
                                                    {
                                                        <span class="badge bg-light text-dark">Pending</span>
                                                    }
                                                </div>
                                                <h6 class="mb-2">@activity.Title</h6>
                                                @if (!string.IsNullOrEmpty(activity.Prompt))
                                                {
                                                    <p class="small text-muted mb-3">@activity.Prompt</p>
                                                }

                                                <!-- Activity Actions -->
                                                <div class="d-flex gap-2 align-items-center">
                                                    @if (activity.Status == Contracts.Enums.ActivityStatus.Pending)
                                                    {
                                                        <!-- Only show Open if no other activity is open -->
                                                        @if (currentActivity == null)
                                                        {
                                                            <button class="btn btn-sm btn-success"
                                                                    @onclick="() => OpenActivity(activity.ActivityId)"
                                                                    disabled="@isPerformingAction">
                                                                <i class="fas fa-play me-1"></i>
                                                                @(isPerformingAction ? "Opening..." : "Start Activity")
                                                            </button>
                                                        }
                                                        else
                                                        {
                                                            <span class="text-muted small">
                                                                <i class="fas fa-lock me-1"></i>Close current activity first
                                                            </span>
                                                        }
                                                    }
                                                    else if (activity.Status == Contracts.Enums.ActivityStatus.Open)
                                                    {
                                                        <!-- Current activity controls shown in top section -->
                                                        <span class="text-success small">
                                                            <i class="fas fa-play-circle me-1"></i>Currently active
                                                        </span>
                                                    }
                                                    else
                                                    {
                                                        <!-- Closed activity -->
                                                        <button class="btn btn-sm btn-outline-primary"
                                                                @onclick="() => ReopenActivity(activity.ActivityId)"
                                                                disabled="@isPerformingAction">
                                                            <i class="fas fa-redo me-1"></i>
                                                            @(isPerformingAction ? "Reopening..." : "Reopen")
                                                        </button>
                                                        <a href="/facilitator/dashboards" class="btn btn-sm btn-outline-secondary">
                                                            <i class="fas fa-chart-bar me-1"></i>Results
                                                        </a>
                                                    }
                                                </div>
                                            </div>
                                        </div>
                                    }

                                    <div class="text-center py-5">
                                        <div class="mb-3">
                                            <i class="fas fa-clipboard-list fa-3x text-muted"></i>
                                        </div>
                                        <p class="text-muted mb-0">No activities yet. Create your first activity to get started.</p>
                                    </div>
                                </div>
                            }
                    </div>

                        <!-- Add Activity Button -->
                        <div class="dropdown w-100">
                            <button class="btn btn-primary pulse-btn-primary w-100 dropdown-toggle"
                                    type="button"
                                    id="addActivityDropdown"
                                    data-bs-toggle="dropdown"
                                    aria-expanded="false">
                                <i class="fas fa-plus me-2"></i>Add Activity
                            </button>
                            <ul class="dropdown-menu w-100" aria-labelledby="addActivityDropdown">
                                <li>
                                    <a class="dropdown-item" href="/facilitator/activities/create-poll?code=@sessionCode">
                                        <strong>📊 Poll</strong>
                                        <br />
                                        <small class="text-muted">Single or multiple choice question</small>
                                    </a>
                                </li>
                                <li><hr class="dropdown-divider" /></li>
                                <li>
                                    <a class="dropdown-item disabled" href="#">
                                        <strong>⭐ Rating</strong>
                                        <br />
                                        <small class="text-muted">Numeric scale (coming soon)</small>
                                    </a>
                                </li>
                                <li>
                                    <a class="dropdown-item disabled" href="#">
                                        <strong>☁️ Word Cloud</strong>
                                        <br />
                                        <small class="text-muted">Short text responses (coming soon)</small>
                                    </a>
                                </li>
                                <li>
                                    <a class="dropdown-item disabled" href="#">
                                        <strong>💬 General Feedback</strong>
                                        <br />
                                        <small class="text-muted">Long-form responses (coming soon)</small>
                                    </a>
                                </li>
                            </ul>
                        </div>
                    </div>
                    }
                </div>
            </div>
        }
      </div>
</main>

@code {
    [SupplyParameterFromQuery]
    public string? Code { get; set; }

    private string sessionCode = string.Empty;
    private SessionSummaryResponse? session;
    private List<AgendaActivityResponse> activities = new();
    private AgendaActivityResponse? currentActivity;
    private int participantCount = 0;
    private string joinUrl = string.Empty;
    private bool isLoading = true;
    private bool isPerformingAction = false;
    private bool isGeneratingQR = false;
    private string errorMessage = string.Empty;

    protected override async Task OnInitializedAsync()
    {
        await LoadSession();
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            // Set page title
            await JS.InvokeVoidAsync("eval", "document.title = 'TechWayFit Pulse — Live Session'");

            if (!string.IsNullOrEmpty(sessionCode) && !string.IsNullOrEmpty(joinUrl))
            {
                try
                {
                    await Task.Delay(300);

                    var canvasId = $"qr-{sessionCode.Replace("-", "")}";
                    var wasAutoGenerated = await JS.InvokeAsync<bool>("eval",
         $"document.getElementById('{canvasId}')?.width > 0 && document.getElementById('{canvasId}')?.getContext('2d').getImageData(0, 0, 1, 1).data[3] !== 0");

                    if (!wasAutoGenerated)
                    {
                        await GenerateQRCode();
                    }
                }
                catch (Exception ex)
                {
                    Logger.LogError(ex, "QR code generation failed");
                    errorMessage = $"QR Code error: {ex.Message}";
                    StateHasChanged();
                }
            }
        }
    }

    private async Task LoadSession()
    {
        try
        {
            isLoading = true;
            errorMessage = string.Empty;
            StateHasChanged();

            // Get session code from query parameter
            if (string.IsNullOrWhiteSpace(Code))
            {
                errorMessage = "No session code provided. Please create a session first.";
                isLoading = false;
                StateHasChanged();
                return;
            }

            sessionCode = Code;

            // Load session data
            session = await ApiService.GetSessionAsync(sessionCode);

            // Load activities
            activities = (await ApiService.GetAgendaAsync(sessionCode)).ToList();
            currentActivity = activities.FirstOrDefault(a => a.Status == Contracts.Enums.ActivityStatus.Open);

            // TODO: Get actual participant count from API
            participantCount = 0;

            // Build join URL
            var uri = new Uri(Navigation.Uri);
            joinUrl = $"{uri.Scheme}://{uri.Authority}/participant/join?code={sessionCode}";
        }
        catch (Exception ex)
        {
            errorMessage = $"Failed to load session: {ex.Message}";
            Logger.LogError(ex, "Failed to load session {Code}", Code);
        }
        finally
        {
            isLoading = false;
            StateHasChanged();
        }
    }

    private async Task GenerateQRCode()
    {
        try
        {
            var canvasId = $"qr-{sessionCode.Replace("-", "")}";
            await JS.InvokeVoidAsync("generateQRCode", canvasId, joinUrl);
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Failed to generate QR code");
            errorMessage = $"QR generation failed: {ex.Message}";
            StateHasChanged();
        }
    }

    private async Task CopyJoinUrl()
    {
        try
        {
            await JS.InvokeVoidAsync("navigator.clipboard.writeText", joinUrl);
            // TODO: Show toast notification
        }
        catch (Exception ex)
        {
            Logger.LogWarning($"Copy failed: {ex.Message}");
        }
    }

    private async Task EndSession()
    {
        try
        {
            isPerformingAction = true;
            StateHasChanged();

            // TODO: Call API to end session
            await Task.Delay(1000); // Placeholder

            Navigation.NavigateTo("/");
        }
        catch (Exception ex)
        {
            errorMessage = $"Failed to end session: {ex.Message}";
        }
        finally
        {
            isPerformingAction = false;
            StateHasChanged();
        }
    }

    private async Task ManualGenerateQR()
    {
        try
        {
            isGeneratingQR = true;
            StateHasChanged();

            errorMessage = string.Empty;
            await Task.Delay(100);

            var canvasId = $"qr-{sessionCode.Replace("-", "")}";
            await JS.InvokeVoidAsync("generateQRCode", canvasId, joinUrl);
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Manual QR generation failed");
            errorMessage = $"Failed to generate QR code: {ex.Message}";
        }
        finally
        {
            isGeneratingQR = false;
            StateHasChanged();
        }
    }

    // Activity Management Methods

    private bool HasPreviousActivity()
    {
        if (currentActivity == null) return false;
        return activities.Any(a => a.Order < currentActivity.Order && a.Status != Contracts.Enums.ActivityStatus.Open);
    }

    private bool HasNextActivity()
    {
        if (currentActivity == null) return false;
        return activities.Any(a => a.Order > currentActivity.Order);
    }

    private async Task OpenActivity(Guid activityId)
    {
        // TODO: Need facilitator token - for now skip
        errorMessage = "Opening activities requires facilitator authentication (coming soon)";
        StateHasChanged();
    }

    private async Task CloseCurrentActivity()
    {
        if (currentActivity == null) return;

        // TODO: Need facilitator token
        errorMessage = "Closing activities requires facilitator authentication (coming soon)";
        StateHasChanged();
    }

    private async Task MoveNext()
    {
        if (currentActivity == null) return;

        isPerformingAction = true;
        errorMessage = string.Empty;

        try
        {
            // 1. Find next pending activity
            var nextActivity = activities
         .Where(a => a.Order > currentActivity.Order && a.Status == Contracts.Enums.ActivityStatus.Pending)
  .OrderBy(a => a.Order)
  .FirstOrDefault();

            if (nextActivity == null)
            {
                errorMessage = "No more activities to move to.";
                return;
            }

            // TODO: Close current and open next via API
            // For now, just show error
            errorMessage = "Move Next requires facilitator authentication (coming soon)";

            // Future implementation:
            // await ApiService.CloseActivityAsync(sessionCode, currentActivity.ActivityId, facilitatorToken);
            // await ApiService.OpenActivityAsync(sessionCode, nextActivity.ActivityId, facilitatorToken);
            // await LoadSession();
        }
        catch (Exception ex)
        {
            errorMessage = $"Failed to move to next activity: {ex.Message}";
            Logger.LogError(ex, "Failed to move to next activity");
        }
        finally
        {
            isPerformingAction = false;
            StateHasChanged();
        }
    }

    private async Task GoBack()
    {
        if (currentActivity == null) return;

        isPerformingAction = true;
        errorMessage = string.Empty;

        try
        {
            // 1. Find previous activity (either pending or closed)
            var previousActivity = activities
             .Where(a => a.Order < currentActivity.Order)
             .OrderByDescending(a => a.Order)
           .FirstOrDefault();

            if (previousActivity == null)
            {
                errorMessage = "No previous activity to go back to.";
                return;
            }

            // TODO: Close current and open previous via API
            errorMessage = "Go Back requires facilitator authentication (coming soon)";

            // Future implementation:
            // await ApiService.CloseActivityAsync(sessionCode, currentActivity.ActivityId, facilitatorToken);
            // await ApiService.OpenActivityAsync(sessionCode, previousActivity.ActivityId, facilitatorToken);
            // await LoadSession();
        }
        catch (Exception ex)
        {
            errorMessage = $"Failed to go back: {ex.Message}";
            Logger.LogError(ex, "Failed to go back to previous activity");
        }
        finally
        {
            isPerformingAction = false;
            StateHasChanged();
        }
    }

    private async Task ReopenActivity(Guid activityId)
    {
        // Can only reopen if no other activity is currently open
        if (currentActivity != null)
        {
            errorMessage = "Close the current activity before reopening another.";
            StateHasChanged();
            return;
        }

        // TODO: Reopen via API
        errorMessage = "Reopening activities requires facilitator authentication (coming soon)";
        StateHasChanged();
    }
}
