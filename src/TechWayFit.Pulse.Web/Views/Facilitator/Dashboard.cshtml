@model IReadOnlyList<TechWayFit.Pulse.Domain.Entities.Session>

@{
    ViewData["Title"] = "Dashboard - TechWayFit Pulse";
    var userEmail = ViewData["UserEmail"] as string;
    var userName = ViewData["UserName"] as string;
    var sessionGroups = ViewData["Groups"] as IEnumerable<TechWayFit.Pulse.Domain.Entities.SessionGroup>;
}

<main class="dashboard-main">
    <div class="container" style="max-width: 1200px;">
        <!-- Header -->
        <div class="dashboard-header">
            <div class="dashboard-header-text">
                <h2 class="dashboard-title">My Workshop Sessions</h2>
                <p class="dashboard-subtitle">Welcome back, <strong>@userName</strong></p>
            </div>
            <div class="dashboard-header-actions">
                <a href="@Url.Action("Groups", "Facilitator")" class="btn btn-pulse-outline">
                    <span class="d-none d-sm-inline">📁 </span>Manage Groups
                </a>
                <a href="@Url.Action("Create", "Facilitator")" class="btn btn-pulse-primary">
                    <span class="d-none d-sm-inline">✨ </span>Create Session
                </a>
            </div>
        </div>

        <!-- Stats Cards -->
        <div class="stat-cards-mobile-wrapper">
            <div class="row g-3 g-md-4 mb-4">
            <div class="col-6 col-lg-3">
                <div class="stat-card">
                    <div class="stat-card-body">
                        <div class="stat-card-content">
                            <div class="stat-card-label">Total Sessions</div>
                            <div class="stat-card-value">@Model.Count</div>
                        </div>
                        <div class="stat-card-icon text-primary">📊</div>
                    </div>
                </div>
            </div>

            <div class="col-6 col-lg-3">
                <div class="stat-card">
                    <div class="stat-card-body">
                        <div class="stat-card-content">
                            <div class="stat-card-label">Live Sessions</div>
                            <div class="stat-card-value">@Model.Count(s => s.Status == TechWayFit.Pulse.Domain.Enums.SessionStatus.Live)</div>
                        </div>
                        <div class="stat-card-icon text-success">📡</div>
                    </div>
                </div>
            </div>

            <div class="col-6 col-lg-3">
                <div class="stat-card">
                    <div class="stat-card-body">
                        <div class="stat-card-content">
                            <div class="stat-card-label">Draft Sessions</div>
                            <div class="stat-card-value">@Model.Count(s => s.Status == TechWayFit.Pulse.Domain.Enums.SessionStatus.Draft)</div>
                        </div>
                        <div class="stat-card-icon text-warning">📝</div>
                    </div>
                </div>
            </div>

            <div class="col-6 col-lg-3">
                <div class="stat-card">
                    <div class="stat-card-body">
                        <div class="stat-card-content">
                            <div class="stat-card-label">Completed</div>
                            <div class="stat-card-value">@Model.Count(s => s.Status == TechWayFit.Pulse.Domain.Enums.SessionStatus.Ended)</div>
                        </div>
                        <div class="stat-card-icon text-secondary">✅</div>
                    </div>
                </div>
            </div>
        </div>
        </div>

        <!-- Collapsible Search and Filter Bar -->
        <div class="filter-toggle-wrapper d-lg-none">
            <button class="filter-toggle-btn" type="button" data-bs-toggle="collapse" data-bs-target="#filterSection" aria-expanded="false" aria-controls="filterSection">
                <span class="filter-toggle-icon">🔍</span>
                <span class="filter-toggle-text">Search & Filter</span>
                <span class="filter-toggle-arrow">▼</span>
            </button>
        </div>

        <div class="collapse d-lg-block" id="filterSection">
            <div class="filter-card">
                <div class="filter-card-body">
                <div class="row g-3 align-items-end">
                    <div class="col-12 col-md-3">
                        <label class="form-label fw-semibold d-none d-md-block">Search</label>
                        <input type="text" class="form-control" id="sessionSearch" placeholder="Search sessions...">
                    </div>
                    <div class="col-12 col-md-3">
                        <label class="form-label fw-semibold d-none d-md-block">Filter by Status</label>
                        <select class="form-select" id="statusFilter">
                            <option value="">All Statuses</option>
                            <option value="Live">Live</option>
                            <option value="Draft">Draft</option>
                            <option value="Ended">Ended</option>
                            <option value="Expired">Expired</option>
                        </select>
                    </div>
                    <div class="col-12 col-md-4">
                        <div>
                            <label class="form-label fw-semibold d-none d-md-block">Filter by Group</label>
                            <div class="flex-grow-1">
                                @{
                                    var treeItems = new List<TechWayFit.Pulse.Web.ViewComponents.TreeDropdown.TreeDropdownItem>();
                                    
                                    if (sessionGroups != null)
                                    {
                                        foreach (var group in sessionGroups)
                                        {
                                            treeItems.Add(new TechWayFit.Pulse.Web.ViewComponents.TreeDropdown.TreeDropdownItem
                                            {
                                                Value = group.Id.ToString(),
                                                Text = group.Name,
                                                ParentValue = group.ParentGroupId?.ToString(),
                                                Level = group.Level - 1,
                                                HasChildren = sessionGroups.Any(g => g.ParentGroupId == group.Id),
                                                Icon = group.Level == 1 ? "📁" : group.Level == 2 ? "📂" : "📄"
                                            });
                                        }
                                    }
                                }
                                @await Component.InvokeAsync("TreeDropdown", new {
                                    id = "groupFilter",
                                    name = "groupFilter",
                                    placeholder = "All Groups",
                                    items = treeItems,
                                    allowClear = true
                                })
                            </div>
                        </div>
                    </div>
                    <div class="col-12 col-md-2">
                        <div class="d-flex gap-2">
                            <button type="button" class="btn btn-primary flex-grow-1" id="applyFilters">
                                <span class="d-none d-md-inline">Apply</span>
                                <span class="d-md-none">🔍</span>
                            </button>
                            <button type="button" class="btn btn-outline-secondary" id="clearFilters">
                                <span class="d-none d-md-inline">Clear</span>
                                <span class="d-md-none">✕</span>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        </div>

        <!-- Sessions Table -->
        <div class="sessions-card">
            <div class="sessions-card-body">
                @if (Model.Any())
                {
                    <!-- Desktop Table View -->
                    <div class="d-none d-lg-block">
                        <table class="sessions-table" id="sessionsTable">
                            <thead>
                                <tr>
                                    <th>Session Code</th>
                                    <th>Title</th>
                                    <th>Group</th>
                                    <th>Status</th>
                                    <th>Created</th>
                                    <th>Expires</th>
                                    <th class="text-end">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var session in Model)
                                {
                                    <tr data-session-id="@session.Id" 
                                        data-group-id="@(session.GroupId?.ToString() ?? "")" 
                                        data-status="@session.Status.ToString()"
                                        data-search-text="@($"{session.Code} {session.Title} {session.Goal ?? ""}").ToLower()">
                                        <td>
                                            <span class="badge bg-primary bg-opacity-10 text-primary border border-primary border-opacity-25 px-3 py-2">
                                                @session.Code
                                            </span>
                                        </td>
                                        <td>
                                            <strong>@session.Title</strong>
                                            @if (!string.IsNullOrEmpty(session.Goal))
                                            {
                                                <br /><small class="text-muted">@session.Goal</small>
                                            }
                                        </td>
                                        <td>
                                            @if (session.GroupId.HasValue && sessionGroups != null)
                                            {
                                                var group = sessionGroups.FirstOrDefault(g => g.Id == session.GroupId.Value);
                                                if (group != null)
                                                {
                                                    <span class="badge bg-secondary bg-opacity-10 text-secondary">
                                                        @group.Name
                                                    </span>
                                                }
                                            }
                                            else
                                            {
                                                <span class="text-muted">—</span>
                                            }
                                        </td>
                                        <td>
                                            @switch (session.Status)
                                            {
                                                case TechWayFit.Pulse.Domain.Enums.SessionStatus.Live:
                                                    <span class="badge bg-success">Live</span>
                                                    break;
                                                case TechWayFit.Pulse.Domain.Enums.SessionStatus.Draft:
                                                    <span class="badge bg-warning text-dark">Draft</span>
                                                    break;
                                                case TechWayFit.Pulse.Domain.Enums.SessionStatus.Ended:
                                                    <span class="badge bg-secondary">Ended</span>
                                                    break;
                                                case TechWayFit.Pulse.Domain.Enums.SessionStatus.Expired:
                                                    <span class="badge bg-danger">Expired</span>
                                                    break;
                                            }
                                        </td>
                                        <td>
                                            <small>@session.CreatedAt.ToString("MMM dd, yyyy")</small>
                                        </td>
                                        <td>
                                            <small class="text-muted">@session.ExpiresAt.ToString("MMM dd, yyyy HH:mm")</small>
                                        </td>
                                        <td class="text-end">
                                            <div class="dropdown">
                                                <button class="btn btn-sm btn-outline-secondary dropdown-toggle" type="button" 
                                                        data-bs-toggle="dropdown" aria-expanded="false">
                                                    <i class="fas fa-ellipsis-v"></i>
                                                </button>
                                                <ul class="dropdown-menu dropdown-menu-end">
                                                    <li>
                                                        <a class="dropdown-item" href="/facilitator/live?Code=@session.Code">
                                                            <i class="fas fa-eye text-primary me-2"></i>View Details
                                                        </a>
                                                    </li>
                                                    <li>
                                                        <a class="dropdown-item" href="#" onclick="editSession('@session.Code'); return false;">
                                                            <i class="fas fa-edit text-info me-2"></i>Edit Details
                                                        </a>
                                                    </li>
                                                    <li>
                                                        <a class="dropdown-item" href="#" onclick="assignGroup('@session.Code', '@session.Id'); return false;">
                                                            <i class="fas fa-folder text-warning me-2"></i>Assign Group
                                                        </a>
                                                    </li>
                                                </ul>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                    
                    <!-- Mobile Card View -->
                    <div class="d-lg-none" id="sessionCards">
                        @foreach (var session in Model)
                        {
                            <div class="session-mobile-card" 
                                 data-session-id="@session.Id" 
                                 data-group-id="@(session.GroupId?.ToString() ?? "")" 
                                 data-status="@session.Status.ToString()"
                                 data-search-text="@($"{session.Code} {session.Title} {session.Goal ?? ""}").ToLower()">
                                <div class="session-mobile-card-header">
                                    <span class="badge bg-primary bg-opacity-10 text-primary border border-primary border-opacity-25 px-3 py-2">@session.Code</span>
                                    @switch (session.Status)
                                    {
                                        case TechWayFit.Pulse.Domain.Enums.SessionStatus.Live:
                                            <span class="badge bg-success">Live</span>
                                            break;
                                        case TechWayFit.Pulse.Domain.Enums.SessionStatus.Draft:
                                            <span class="badge bg-warning text-dark">Draft</span>
                                            break;
                                        case TechWayFit.Pulse.Domain.Enums.SessionStatus.Ended:
                                            <span class="badge bg-secondary">Ended</span>
                                            break;
                                        case TechWayFit.Pulse.Domain.Enums.SessionStatus.Expired:
                                            <span class="badge bg-danger">Expired</span>
                                            break;
                                    }
                                </div>
                                <div class="session-mobile-card-body">
                                    <h6 class="session-mobile-title">@session.Title</h6>
                                    @if (!string.IsNullOrEmpty(session.Goal))
                                    {
                                        <p class="session-mobile-goal">@session.Goal</p>
                                    }
                                    <div class="session-mobile-meta">
                                        @if (session.GroupId.HasValue && sessionGroups != null)
                                        {
                                            var group = sessionGroups.FirstOrDefault(g => g.Id == session.GroupId.Value);
                                            if (group != null)
                                            {
                                                <span class="session-mobile-group">📁 @group.Name</span>
                                            }
                                        }
                                        <span class="session-mobile-date">Created: @session.CreatedAt.ToString("MMM dd, yyyy")</span>
                                    </div>
                                </div>
                                <div class="session-mobile-card-footer">
                                    <div class="dropdown w-100">
                                        <button class="btn btn-sm btn-outline-secondary dropdown-toggle w-100" type="button" 
                                                data-bs-toggle="dropdown" aria-expanded="false">
                                            <i class="fas fa-ellipsis-v"></i> Actions
                                        </button>
                                        <ul class="dropdown-menu dropdown-menu-end w-100">
                                            <li>
                                                <a class="dropdown-item" href="/facilitator/live?Code=@session.Code">
                                                    <i class="fas fa-eye text-primary me-2"></i>View Details
                                                </a>
                                            </li>
                                            <li>
                                                <a class="dropdown-item" href="#" onclick="editSession('@session.Code'); return false;">
                                                    <i class="fas fa-edit text-info me-2"></i>Edit Details
                                                </a>
                                            </li>
                                            <li>
                                                <a class="dropdown-item" href="#" onclick="assignGroup('@session.Code', '@session.Id'); return false;">
                                                    <i class="fas fa-folder text-warning me-2"></i>Assign Group
                                                </a>
                                            </li>
                                        </ul>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                    
                    <div class="session-count-badge">
                        <span id="sessionCount">
                            @Model.Count @(Model.Count == 1 ? "session" : "sessions")
                        </span>
                    </div>
                }
                else
                {
                    <div class="text-center py-5">
                        <div class="mb-4">
                            <svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" fill="currentColor" class="text-muted" viewBox="0 0 16 16">
                                <path d="M8 15A7 7 0 1 1 8 1a7 7 0 0 1 0 14zm0 1A8 8 0 1 0 8 0a8 8 0 0 0 0 16z" />
                                <path d="M8 4a.5.5 0 0 1 .5.5v3h3a.5.5 0 0 1 0 1h-3v3a.5.5 0 0 1-1 0v-3h-3a.5.5 0 0 1 0-1h3v-3A.5.5 0 0 1 8 4z" />
                            </svg>
                        </div>
                        <p class="text-muted mb-3">You haven't created any sessions yet</p>
                        <a href="@Url.Action("Create", "Facilitator")" class="btn btn-primary pulse-btn-primary">
                            Create Your First Session
                        </a>
                    </div>
                }
            </div>
        </div>
    </div>
</main>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const searchInput = document.getElementById('sessionSearch');
            const groupFilter = document.getElementById('groupFilter');
            const statusFilter = document.getElementById('statusFilter');
            const applyBtn = document.getElementById('applyFilters');
            const clearBtn = document.getElementById('clearFilters');
            const sessionCount = document.getElementById('sessionCount');
            
            // Apply filters
            applyBtn.addEventListener('click', function() {
                applyFilters();
            });
            
            // Clear filters
            clearBtn.addEventListener('click', function() {
                searchInput.value = '';
                groupFilter.value = '';
                statusFilter.value = '';
                document.querySelector('[data-dropdown-id="groupFilter"] .dropdown-text').textContent = 'All Groups';
                applyFilters();
            });
            
            // Tree dropdown functionality
            initializeTreeDropdown();
            
            function applyFilters() {
                const searchText = searchInput.value.toLowerCase();
                const selectedGroupId = groupFilter.value;
                const selectedStatus = statusFilter.value;
                const rows = document.querySelectorAll('#sessionsTable tbody tr');
                const cards = document.querySelectorAll('.session-mobile-card');
                let visibleCount = 0;
                
                // Filter desktop table rows
                rows.forEach(row => {
                    const rowSearchText = row.getAttribute('data-search-text');
                    const rowGroupId = row.getAttribute('data-group-id');
                    const rowStatus = row.getAttribute('data-status');
                    
                    let showRow = true;
                    
                    // Apply search filter
                    if (searchText && !rowSearchText.includes(searchText)) {
                        showRow = false;
                    }
                    
                    // Apply group filter
                    if (selectedGroupId && rowGroupId !== selectedGroupId) {
                        showRow = false;
                    }
                    
                    // Apply status filter
                    if (selectedStatus && rowStatus !== selectedStatus) {
                        showRow = false;
                    }
                    
                    if (showRow) {
                        row.style.display = '';
                        visibleCount++;
                    } else {
                        row.style.display = 'none';
                    }
                });
                
                // Filter mobile cards
                cards.forEach(card => {
                    const cardSearchText = card.getAttribute('data-search-text');
                    const cardGroupId = card.getAttribute('data-group-id');
                    const cardStatus = card.getAttribute('data-status');
                    
                    let showCard = true;
                    
                    // Apply search filter
                    if (searchText && !cardSearchText.includes(searchText)) {
                        showCard = false;
                    }
                    
                    // Apply group filter
                    if (selectedGroupId && cardGroupId !== selectedGroupId) {
                        showCard = false;
                    }
                    
                    // Apply status filter
                    if (selectedStatus && cardStatus !== selectedStatus) {
                        showCard = false;
                    }
                    
                    card.style.display = showCard ? '' : 'none';
                });
                
                // Update count
                sessionCount.textContent = `${visibleCount} ${visibleCount === 1 ? 'session' : 'sessions'}`;
            }
            
            function initializeTreeDropdown() {
                // Handle tree toggle clicks first with capture phase
                document.addEventListener('click', function(e) {
                    // Tree toggle functionality - handle in capture phase to prevent dropdown from closing
                    if (e.target.closest('.tree-toggle')) {
                        const toggle = e.target.closest('.tree-toggle');
                        const targetId = toggle.getAttribute('data-target');
                        const target = document.getElementById(targetId);
                        const icon = toggle.querySelector('i');
                        
                        if (target) {
                            if (target.style.display === 'none' || target.style.display === '') {
                                target.style.display = 'block';
                                icon.className = 'fas fa-chevron-down';
                            } else {
                                target.style.display = 'none';
                                icon.className = 'fas fa-chevron-right';
                            }
                        }
                        
                        e.preventDefault();
                        e.stopPropagation();
                        e.stopImmediatePropagation();
                        return false;
                    }
                }, true); // Use capture phase
                
                // Handle tree item selection in normal phase
                document.addEventListener('click', function(e) {
                    // Only handle tree item selection if not clicking on toggle
                    if (e.target.closest('.tree-item') && !e.target.closest('.tree-toggle')) {
                        const treeItem = e.target.closest('.tree-item');
                        const value = treeItem.getAttribute('data-value');
                        const text = treeItem.querySelector('.tree-item-text').textContent;
                        const dropdown = treeItem.closest('.tree-dropdown');
                        const hiddenInput = dropdown.querySelector('input[type="hidden"]');
                        const dropdownText = dropdown.querySelector('.dropdown-text');
                        
                        // Update value
                        hiddenInput.value = value;
                        dropdownText.textContent = value ? text : 'All Groups';
                        
                        // Close dropdown only when selecting an item
                        const dropdownElement = new bootstrap.Dropdown(dropdown.querySelector('[data-bs-toggle="dropdown"]'));
                        dropdownElement.hide();
                        
                        // Remove selected class from all items
                        dropdown.querySelectorAll('.tree-item').forEach(item => {
                            item.classList.remove('selected');
                        });
                        
                        // Add selected class to clicked item
                        if (value) {
                            treeItem.classList.add('selected');
                        }
                        
                        e.preventDefault();
                        e.stopPropagation();
                    }
                });
            }
        });
        
        // Context menu functions
        function editSession(sessionCode) {
            // TODO: Implement edit functionality
            alert('Edit functionality will be implemented for session: ' + sessionCode);
        }
        
        function assignGroup(sessionCode, sessionId) {
            // TODO: Implement assign group functionality
            alert('Assign group functionality will be implemented for session: ' + sessionCode);
        }
    </script>
}
