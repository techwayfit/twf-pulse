@{
    ViewBag.Title = "Access Denied";
    ViewBag.ActiveNav = "";
}

<div class="bo-empty-state">
    <svg class="bo-empty-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
        <circle cx="12" cy="12" r="10" />
        <line x1="4.93" y1="4.93" x2="19.07" y2="19.07" />
    </svg>
    <div class="bo-empty-title">Access Denied</div>
    <p class="bo-empty-desc">You do not have the required role to access this page.</p>
    <a asp-controller="Dashboard" asp-action="Index" class="bo-btn bo-btn-ghost">&larr; Return to Dashboard</a>
</div>
